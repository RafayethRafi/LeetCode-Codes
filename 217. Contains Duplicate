class Solution {
public:
    bool containsDuplicate(vector<int>& nums) {
        
        int x = nums.size();
        
        set<int>s;
        
        for(int i=0;i<x;i++)
            s.insert(nums[i]);
        
        if(s.size()==x)
            return false;
        else 
            return true;
        
    }
};
